import { Plugin } from 'vite';

type UsePHPConfig = {
    binary?: string;
    entry?: string | string[];
    tempDir?: string;
    cleanup?: {
        dev?: boolean;
        build?: boolean;
    };
};
declare function usePHP(cfg?: UsePHPConfig): Plugin[];

export { usePHP as default };
